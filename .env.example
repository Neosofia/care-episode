JWT_AUDIENCE=care-episode
ENV=development
LOG_LEVEL=info
PORT=8015

MIGRATION_DATABASE_URL=
APP_DATABASE_URL=

JWT_JWKS_URI=http://authentication:8014/.well-known/jwks.json
FRONTEND_URL=http://localhost:5173

# Chat proxy — service-to-service auth and registry discovery
# Chat base URL is resolved from the authentication service registry (slug: chat).
CARE_EPISODE_CLIENT_SECRET=
# Clinical risk inference (OpenAI-compatible completions API, e.g. Bedrock gateway)
INFERENCE_COMPLETIONS_URL=
INFERENCE_API_KEY=
INFERENCE_MODEL=
# INFERENCE_TEMPERATURE=0.2
# RISK_ESCALATION_ENABLED=true
# CLINICAL_RISK_ALERT_FROM_EMAIL=care-episode-alerts@neosofia.tech

# Optional overrides (defaults shown):
# WEB_CONCURRENCY=1
# GUNICORN_THREADS=32
# GUNICORN_TIMEOUT=15
# AUTHENTICATION_SERVICE_BASE_URL=http://authentication:8014
# AUTHENTICATION_TOKEN_TIMEOUT_SECONDS=10
# CHAT_SERVICE_TIMEOUT_SECONDS=10
# INFERENCE_TIMEOUT_SECONDS=10
# SERVICE_REGISTRY_CACHE_TTL_SECONDS=60
# NOTIFICATION_SERVICE_TIMEOUT_SECONDS=10
